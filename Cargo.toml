[package]
name = "graph_matching"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"
opencv = { version = "0.98", default-features = false, features = ["imgproc"] }

[dev-dependencies]
testresult = "0.4"
opencv = { version = "0.98", default-features = false, features = ["imgproc", "imgcodecs"] }

[features]
profile = ["opencv/imgcodecs"]
